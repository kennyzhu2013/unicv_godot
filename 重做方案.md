
源码里有两个决定迁移路线的关键点：现有 `server` 主要负责存档传输和聊天，并不执行游戏规则；规则和 AI 都在客户端 `core` 中，而且部分单位操作与 UI 混在一起。因此，用 Godot 重做前端需要先建立逻辑接口，不能只对接现有服务器。我会按“保留 Kotlin 逻辑”和“全部移植到 Godot”两条路线分析。
当前代码已有 `GameView / TileView / MapUnitView` 视图层，可以作为 Godot 接口的起点。但它还不是完整的跨进程 API：部分方法仍暴露底层对象，动作校验也未全部收口。另外，规则 JSON 中的 `uniques` 是由 Kotlin 解析执行的规则表达式，仅复制 JSON 和图片无法复现玩法。
我按“用 Godot 重做前端，尽量复刻 Unciv 的现有玩法”来理解。

结合当前源码，我的结论是：**可以做，而且如果重点是重做画面和交互，最合适的起步方式是“Godot 前端 + 保留 Kotlin 游戏逻辑”，而不是立即重写整个游戏。**

但这里有个重要前提：Unciv 不是现成的“前端调用游戏服务器”架构。要换 Godot，必须先补出一层真正的游戏接口。

以下是源码分析与实施建议，本次没有修改代码，也没有实际运行 Godot 接入验证。

## 一、目前的游戏架构意味着什么
### 1. 游戏规则主要在客户端，不在 server

当前结构可以简化为：

```text
desktop / android
    └── 启动、平台适配
          └── core
                ├── logic：游戏状态、回合、城市、战斗、AI
                ├── models/ruleset：规则数据与特殊效果
                ├── view：面向玩家的访问接口，尚未完全收口
                └── ui：LibGDX 界面、地图渲染、交互

server
    └── 联机存档传输、认证、聊天
```

你现在打开的 [UncivServer.kt](file:///d:/game/unicv_godot/Unciv-master/server/src/com/unciv/app/server/UncivServer.kt#L320-L438)，主要提供：

- `GET /isalive`
- `PUT /files/{fileName}`
- `GET /files/{fileName}`
- 认证接口
- 聊天 WebSocket

它没有负责单位移动、战斗结算、城市生产或 AI 决策。

所以，**Godot 即使成功接上当前 server，也只是获得了存档传输能力，还不能运行游戏。**

### 2. 游戏状态已经有比较清楚的模型

核心是 [GameInfo](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/logic/GameInfo.kt#L100-L180)：

```text
GameInfo
├── civilizations：文明实例
│   ├── cities：城市
│   ├── 科技、政策、外交、宗教等管理器
│   └── 单位管理
├── tileMap：地图
│   └── Tile：地形、资源、改良设施、单位
├── gameParameters：规则集、速度、胜利条件等
├── currentPlayer：当前行动方
└── turns：轮次
```

还有一类重要对象是 `Ruleset`：单位类型、科技、建筑、文明、地形等定义。

这个区分值得直接沿用：

- `BaseUnit` 是“勇士这种兵种”。
- `MapUnit` 是“地图上这一支勇士”。
- `Nation` 是文明定义。
- `Civilization` 是本局中的文明实例。

在 Godot 中也应该分开“定义数据”“运行状态”和“显示节点”，不要把全部状态塞进 `Sprite2D` 或场景树。

### 3. “下一回合”不是简单地 turns 加一

[GameInfo.nextTurn()](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/logic/GameInfo.kt#L377-L496) 的主要流程是：

```text
结束当前人类玩家的回合
    ↓
切换文明
    ↓
如果需要自动处理：
    开始回合 → AI 行动 → 结束回合
    ↓
继续切换，直到轮到需要玩家操作的文明
    ↓
初始化该玩家的回合
```

所有文明轮转一圈，`turns` 才增加。

城市、单位、科技、外交等结算又分散在文明、城市、单位的回合管理器中，顺序影响结果。例如金币结算、科研进度、城市生产、临时效果到期、单位状态更新不能随意交换。

原前端在 [WorldScreen.nextTurn()](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/ui/screens/worldscreen/WorldScreen.kt#L605-L695) 中还会：

1. 克隆游戏状态。
2. 重建运行时引用。
3. 在后台线程推进回合。
4. 联机时上传新状态。
5. 切换到新状态对应的界面。

这也是新前端应保留的思路：**逻辑计算与显示状态隔离，计算期间界面仍能响应。**

## 二、哪些能复用，哪些必须重做

| 部分 | Godot 前端方案中的处理 |
|---|---|
| 城市、科技、战斗、外交规则 | 保留 Kotlin，通过接口调用 |
| AI、地图生成、寻路 | 优先保留 |
| 原存档读写 | 交给 Kotlin 内核处理 |
| 规则 JSON | 保留，由原加载器解释或导出标准化数据 |
| PNG 图片 | 可以导入，但需重建资源映射与图集 |
| 地图渲染 | 用 Godot 重写 |
| 菜单、面板、弹窗 | 用 Godot Control 系统重写 |
| 音效与音乐播放 | Godot 接管播放，逻辑层发出提示 |
| 原联机存档服务 | 按兼容目标决定是否继续使用 |

有两个容易低估的地方。

### 1. JSON 不等于完整游戏规则

例如 [Units.json](file:///d:/game/unicv_godot/Unciv-master/android/assets/jsons/Civ%20V%20-%20Gods%20%26%20Kings/Units.json#L5-L23) 中，开拓者包含：

```text
Founds a new city <by consuming this unit>
Requires at least [2] population
```

这些不是普通说明文字，而是会被解释执行的 `uniques`。

[Unique.kt](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/models/ruleset/unique/Unique.kt#L19-L98) 会解析：

- 效果类型；
- 参数；
- 条件与修饰符；
- 触发属性；
- 数值信息。

具体行为还涉及 `UniqueType`、`Conditionals`、`UniqueTriggerActivation` 及各玩法模块。

因此，复制 JSON 只能拿到定义，不能自动得到建城、加成、特殊能力和 Mod 兼容性。

另外，原始规则文件含注释，加载后还会进行默认值补全、科技列处理、Mod 合并和派生计算，不能直接当成普通 JSON 字典照搬。

### 2. logic 与 ui 还没有彻底解耦

具体例子：

- `GameInfo.nextTurn()` 接受 UI 进度条对象，并访问音乐、自动播放状态。
- `GameStarter` 创建游戏后会保存客户端设置。
- [UnitActions](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/ui/screens/worldscreen/unit/actions/UnitActions.kt#L18-L95) 在 UI 目录，却同时提供玩家、AI 和测试使用的动作逻辑。
- 部分单位动作会打开确认弹窗、读取当前选择单位。
- AI 的升级、劫掠也会调用 UI 目录中的动作实现。

所以不能直接删除整个 `ui` 目录，再期待 `logic` 独立运行。需要按调用链逐步拆分。

## 三、推荐哪条路线

| 路线 | 适用情况 | 主要代价 |
|---|---|---|
| Godot + 本地 Kotlin/JVM 进程 | 桌面优先、离线、追求规则一致 | 需要逻辑网关及 JVM 打包 |
| Godot + 远程 Kotlin 服务 | 在线游戏、希望服务端掌握规则 | 需要部署、权限、会话隔离，依赖网络 |
| Godot 内原生重写规则 | 要独立客户端、长期摆脱 JVM | 重写与回归工作最大 |
| Godot 内嵌 JVM／语言桥接 | 已有相关技术积累 | 插件、线程和平台维护成本较高 |

**对于“重做前端”这个目标，我推荐先走第一条，桌面端完成验证。**

```text
Godot
├── 地图、单位动画
├── 城市和科技等界面
├── 输入状态机
└── 游戏接口客户端
          │
          │ 本机 HTTP / WebSocket
          ▼
新增 Kotlin 游戏网关
├── 命令校验
├── 查询和显示数据转换
├── 单会话执行队列
└── Unciv core
      ├── 状态、规则
      ├── AI
      └── 存档
```

本地进程可以随游戏启动，离线运行，不必要求玩家连接云服务。

注意两个边界：

- 这里的“游戏网关”是新增组件，不是当前存档服务器直接改个名字。
- 桌面本地 JVM 方案不能原样套到移动端。Android 需要另做插件接入验证；iOS 和浏览器也不能按桌面启动 JVM 子进程的方式处理。

如果你的硬要求是“首版就纯 Godot、移动端离线、没有 JVM”，则应该直接选择原生移植路线，并主动缩小首版玩法范围。

## 四、真正关键的是接口怎么设计

### 1. 利用已有 View 层，但不能直接序列化它

当前分支已经有：

- `GameView`
- `CivView`
- `CityView`
- `TileView`
- `MapUnitView`
- 对应的外国文明、城市、单位视图

[View.kt](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/view/View.kt#L6-L23) 明确希望限制玩家可以读取的数据和执行的动作。这是非常合适的改造起点。

但目前仍有不足：

- 一些方法暴露底层游戏对象。
- 部分 `try...` 方法直接执行操作，没有完整前置校验。
- 例如 `TileView` 仍有原始资源字段，不能认为所有字段天然经过战争迷雾过滤。
- 它们是 JVM 内部对象，不是跨进程协议。

建议在 View 外再加明确的 DTO 和命令处理层。

### 2. 分开查询与命令

以下是建议新增的接口职责，不是源码现有接口：

| 查询 | 返回内容 |
|---|---|
| 获取当前玩家快照 | 地图可见信息、单位、城市、文明概况 |
| 获取单位操作 | 可执行操作、参数选项、禁用原因 |
| 查询移动路线 | 路径、移动消耗、预计回合 |
| 查询战斗预览 | 可攻击性、预计伤害、加成明细 |
| 获取城市详情 | 产出、生产队列、人口、可建项目 |
| 查询回合状态 | 待决策事项、是否正在结算 |

命令则包括：

```text
移动单位、攻击、建城、开始改良设施
设置生产队列、选择科技、采用政策
回应外交或事件选择、结束回合
```

不要让 Godot 发出“把单位坐标设为某值”这种直接修改状态的请求，而应该表达“请求将这个单位移动到目标格”。

合法性、费用及副作用由 Kotlin 统一处理。

### 3. 每次操作都需要版本与执行校验

建议命令携带：

```text
协议版本
会话 ID
请求 ID
预期状态版本
动作类型
单位／城市 ID
目标与选项
```

网关应当：

- 根据会话身份确定操作者，而不是相信请求自报的文明。
- 校验行动方、对象归属、行动力、资源、目标合法性。
- 使用请求 ID 防止重试导致重复执行。
- 拒绝基于过期状态的命令。
- 返回新状态版本和结果。

原代码有 `UncivGame.Current` 等全局对象。初期最稳妥的是**一个逻辑进程管理一个游戏会话，命令串行执行**。不能简单在 Ktor 中并发处理多个对局。

### 4. 存档、显示快照、动画事件分开

建议明确三种数据：

```text
SaveGame：完整持久化数据，只由内核管理
PlayerSnapshot：玩家有权看到的数据，供 Godot 显示
PresentationEvents：移动、受伤、建成、通知等表现事件
```

原因是：

- 存档没有保存所有运行时引用和派生结果。
- 完整存档含其他玩家的隐藏信息。
- 单看最终状态，无法还原“先移动、再攻击、再死亡”的动画过程。

现有 [GameInfo.setTransients()](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/logic/GameInfo.kt#L733-L811) 会重建规则、文明、地图、单位等关联。Godot 前端不应该再实现一遍这些逻辑。

首版可以每次成功操作后返回完整的玩家快照；性能数据出来后，再引入增量更新。不要一开始就要求内核准确维护复杂的变化集。

### 5. 别遗漏“等待玩家选择”

原来的“下一回合”按钮实际还负责引导：

- 选择生产项目；
- 选择科技或政策；
- 选择宗教信条；
- 处理免费伟人；
- 操作闲置单位等。

对应逻辑在 [NextTurnAction.kt](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/ui/screens/worldscreen/status/NextTurnAction.kt#L52-L145)。

新网关需要返回“待处理事项”，区分必须完成的选择与可忽略的提醒。Godot 再决定打开哪个面板，不能只暴露一个 `nextTurn()` 按钮。

## 五、Godot 前端具体怎么做

### 1. 地图采用分层结构

原来的 [TileGroupMap](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/ui/components/tilegroups/TileGroupMap.kt#L113-L179) 是按全地图图层绘制，而不是逐格画完所有内容。

Godot 可以对应为：

```text
WorldScreen
├── MapRoot：Node2D
│   ├── TerrainLayer
│   ├── TerrainFeatureLayer
│   ├── RiversAndRoads
│   ├── Borders
│   ├── ResourcesAndImprovements
│   ├── Units
│   ├── FogAndHighlights
│   └── CityLabels
├── Camera2D
└── HUD：CanvasLayer
    ├── 顶部资源栏
    ├── 单位操作面板
    ├── 地块详情
    ├── 战斗预览
    ├── 小地图
    └── 回合按钮
```

建议：

- 常规地形用六边形 `TileMapLayer`。
- 河流、道路、边界用额外图层或自定义绘制。
- 单位用 `Node2D + Sprite2D`。
- 城市标签和操作面板用 `Control`。
- 高亮、路线、攻击范围独立绘制。

Godot 当前的 [TileMapLayer 文档](https://docs.godotengine.org/en/stable/classes/class_tilemaplayer.html) 推荐通过多个单层节点组织地图。

但原素材的叠加、边缘和变体规则不一定能直接对应 Godot 自动地形连接，仍需要自己的资源选择逻辑。

### 2. 六边形坐标必须做适配

[HexMath.kt](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/logic/map/HexMath.kt#L101-L172) 使用的是：

- `x` 向左上增加；
- `y` 向右上增加；
- `(1,1)` 向正上方移动。

不能直接把这两个整数当成 Godot 的地图行列。

建议保留原始逻辑坐标，增加：

```text
Unciv 坐标 ↔ Godot 地图格坐标 ↔ 显示坐标
```

忽略原界面偏移，设六边形外接圆半径为 `R`，按照原朝向转换到 Godot 的 Y 轴向下坐标，可以写成：

```text
屏幕 X = 1.5 × R × (y - x)
屏幕 Y = -√3 / 2 × R × (x + y)
```

需要专门验证：

- 六个邻居是否对应；
- 点击定位是否准确；
- 负坐标；
- 地图环绕；
- 跨边界路线；
- 河流和道路方向。

### 3. 寻路结果由内核提供

不能用 Godot 普通寻路直接替代 Unciv 寻路。

[MovementCost.kt](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/logic/map/mapunit/movement/MovementCost.kt#L40-L124) 已经考虑了：

- 上船与登陆；
- 控制区；
- 道路、铁路、跨河；
- 地形与文明能力；
- 护送；
- 特殊移动加成。

Godot 的职责是“显示内核返回的路径，并播放动画”，不再计算一套可能不同的移动规则。

### 4. 战争迷雾不仅是画一层黑色

至少要表达：

1. 未探索；
2. 已探索但当前不可见；
3. 当前可见。

还涉及资源是否已被科技揭示、隐形单位是否被侦测、改良设施的最后已知状态。

新网关必须按玩家权限生成快照，不能把全部数据发出去，再指望 Godot 把敌人隐藏。

动画事件和移动历史也需要过滤，源码已有相应的 [可见性测试](file:///d:/game/unicv_godot/Unciv-master/tests/src/com/unciv/view/GameViewMovementHistoryTest.kt#L27-L85)，可以作为基准。

### 5. 素材可以复用，但要保留组合语义

一个非常具体的坑：`Warrior-1.png`、`Warrior-2.png` 不应直接当成动画帧。

[ImageGetter.getLayeredImageColored()](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/ui/images/ImageGetter.kt#L151-L186) 会将数字后缀图片作为叠加层，分别着色。

Godot 中应当用多个 Sprite 或着色器复现：

```text
单位底图 + 文明主色层 + 文明副色层
```

同时保留：

- 文明专属变体；
- 时代、文化风格变体；
- 地形组合；
- 缺失素材回退。

首版建议只支持一套地图皮肤和单位皮肤，再扩展完整资源兼容。

### 6. 性能目标要落实在显示架构

建议从一开始避免：

- 每个地块一个复杂场景；
- 每格创建物理碰撞体；
- 每帧重新计算产出；
- 每帧请求逻辑服务；
- 回合后销毁重建整个地图。

更合适的是：

- 点击通过坐标数学定位；
- 地形批量绘制；
- 单位和城市按稳定 ID 更新；
- 只更新变化数据对应的表现；
- 视口外隐藏复杂标签；
- 后台处理通信，主线程更新场景树。

换 Godot 本身不会自动加快 AI 回合；保留 Kotlin 内核时，逻辑计算耗时基本还是原来的问题。

## 六、如果要完全移植成纯 Godot

这就不只是换前端，而是重写游戏内核。

桌面为主时，可以考虑：

```text
Godot UI
    ↓
应用接口
    ↓
独立 C# 规则库
├── 游戏状态
├── 规则加载
├── 回合结算
├── 移动、战斗、城市
├── AI
└── 存档
```

规则库尽量不依赖 Godot `Node`，这样才能独立测试。

需要注意：当前 Godot 官方文档仍说明 [C# 项目不能导出 Web，移动端支持也有实验性限制](https://docs.godotengine.org/en/stable/tutorials/scripting/c_sharp/index.html)。如果浏览器是硬目标，应先据此确定语言和运行位置。

原生移植最难的不是语法转换，而是：

- `uniques` 的完整语义；
- 回合与触发效果的顺序；
- 数值精度、取整、集合遍历顺序；
- AI 决策；
- 原存档及 Mod 兼容；
- 随机行为一致性。

例如 [GameContext.stateBasedRandom()](file:///d:/game/unicv_godot/Unciv-master/core/src/com/unciv/models/ruleset/unique/GameContext.kt#L89-L149) 会结合上下文哈希生成随机数。仅在 Godot 中设置相同 seed，并不能保证与 Kotlin 得到相同结果。

如果采用这条路线，应先声明支持的规则子集，对未支持的规则报错，而不是静默忽略。

## 七、建议按这个顺序落地

| 阶段 | 做什么 | 验收重点 |
|---|---|---|
| 1. 内核可运行验证 | 无界面加载规则和存档，执行移动、建城、下一回合 | 找出会触发 UI 的真实调用链 |
| 2. Godot 地图查看器 | 通过玩家快照显示地图、单位、城市、迷雾 | 坐标、图层、着色与信息可见性 |
| 3. 最小可玩闭环 | 选单位、移动、建城、生产、科研、结束回合 | 能连续游玩并存档重载 |
| 4. 战斗与 AI | 战斗预览、攻击、死亡、占城、AI 回合 | 状态结算与表现一致 |
| 5. 完整体验 | 外交、政策、宗教、事件、胜利等 | 所有待决策流程可完成 |
| 6. 兼容与发布 | Mod、旧存档、原联机协议、各平台打包 | 分别测试，不默认兼容 |

第一阶段可以借鉴现有 [BaseTestRunner](file:///d:/game/unicv_godot/Unciv-master/tests/src/com/unciv/testing/BaseTestRunner.kt#L17-L50) 的无界面文件环境和 `TestGame` 初始化方式。但测试基础设施只能说明存在可借鉴的运行路径，不代表整个游戏已经可以无修改作为服务运行。

验证应当分三层：

- **规则回归**：复用移动、战斗、城市、uniques 等已有测试。
- **接口测试**：非法命令、重复请求、过期版本、隐藏信息、断线恢复。
- **端到端测试**：同一初始存档、同一操作序列，对比关键游戏状态。

跨语言移植时要单独控制随机条件；对比状态时也应排除时间戳等非玩法字段。

## 八、许可需要提前确认

当前仓库的 [LICENSE](file:///d:/game/unicv_godot/Unciv-master/LICENSE#L1-L22) 是 **MPL 2.0**。使用和修改受覆盖的源码并分发时，需要履行相应源码提供与声明义务。

图片、图标等并非统一许可，[Credits](file:///d:/game/unicv_godot/Unciv-master/docs/Credits.md#L3-L30) 列出了 CC0、CC BY、CC BY-SA 等不同来源。不能因为更换了引擎，就认为源码与素材的许可要求消失。

最后归纳：**如果你的目标是“玩法不变，换成 Godot 的画面与交互”，先保留 Kotlin 内核，围绕现有 View 层建立命令和玩家快照接口，再重做 Godot 前端。**

最值得先做的不是完整菜单，而是验证这一条闭环：

**读取现有存档 → Godot 显示地图 → 移动单位 → 建城 → 选择生产 → AI 推进回合 → 保存并重载。**

这条链路跑通后，才能比较可靠地判断后续界面工作量，以及是否值得继续向纯 Godot 内核迁移。